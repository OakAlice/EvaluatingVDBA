# Formatting each of the raw data sources into standard structure ---------
# And generate the VDBA for each time instance

# Wanja_Fox
source(file = file.path(base_path, "Scripts", "Formatting", "Wanja_Fox_Processing.R"))

